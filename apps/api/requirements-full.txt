# Add-on dependencies for the full Docker stack:
#   - Postgres (psycopg)
#   - Qdrant (qdrant-client)
#   - Redis (redis)
#   - Neo4j (neo4j)
#   - Celery (celery + alembic for migrations)
#   - LlamaParse (better PDF parser)
#   - Optional alternate LLM providers (Anthropic, Gemini)
#   - Local sentence-transformer embeddings (heavy: pulls torch)
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-full.txt

psycopg[binary]==3.2.3
alembic==1.13.3
redis==5.1.1
celery==5.4.0
qdrant-client==1.12.0
neo4j==5.25.0

anthropic==0.39.0
google-generativeai==0.8.3

llama-parse==0.5.10
sentence-transformers==3.1.1
