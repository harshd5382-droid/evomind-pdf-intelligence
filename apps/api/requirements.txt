# Minimum requirements for the NVIDIA-only zero-infra dev mode.
# (SQLite + in-memory vector store + in-memory pubsub.)
# For the full Docker stack with Postgres / Qdrant / Redis / Neo4j / Celery,
# also install requirements-full.txt.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
sqlalchemy>=2.0.35
httpx>=0.27.0
python-multipart>=0.0.12
python-dotenv>=1.0.0
tenacity>=9.0.0
loguru>=0.7.2
slowapi>=0.1.9

# LLM provider (NVIDIA NIM uses the OpenAI SDK against build.nvidia.com)
openai>=1.51.0

# PDF processing (versions chosen for prebuilt wheels on Python 3.13)
pymupdf>=1.25.0
pdfplumber>=0.11.4

# Math / parsing / retrieval
sympy>=1.13.0
numpy>=1.26.0
rank-bm25>=0.2.2
