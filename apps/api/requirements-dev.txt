# Developer / CI tooling. Not needed at runtime.
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Keeps the runtime image lean: tests, linting, type-checking, and the
# dependency CVE audit all live here, separate from requirements.txt.

pytest>=8.3.0
pytest-cov>=5.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0          # FastAPI TestClient transport (also a runtime dep)

ruff>=0.7.0            # linter + formatter
mypy>=1.13.0           # static type-checking
pip-audit>=2.7.0       # dependency vulnerability scan
