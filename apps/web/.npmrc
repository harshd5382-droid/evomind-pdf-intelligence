; next@15.0.3 declares its React 19 peer as an RC version, but this project
; uses React 19.0.0 stable. Allow npm to resolve that mismatch so `npm ci`
; and `npm install` work without --legacy-peer-deps on the command line.
legacy-peer-deps=true
