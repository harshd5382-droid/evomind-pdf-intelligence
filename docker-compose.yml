# Passwords and API keys come from the environment (.env) with dev-only
# fallbacks. For any non-local deployment set POSTGRES_PASSWORD / NEO4J_PASSWORD
# and, if exposing the API, AUTH_ENABLED=true + API_KEYS=<comma-separated>.
#
# Configuration reaches the containers two ways:
#   1. `env_file: .env`  — every knob in .env.example (autopilot cadences,
#      QUESTIONS_PER_DOC, INGEST_WORKERS, PARSER_PRIORITY, ...). The image
#      contains no .env of its own, so without this they silently fall back to
#      the code defaults in app/core/config.py.
#   2. `environment: <<: *api-env` — infra wiring that MUST point at compose
#      service names. `environment` takes precedence over `env_file`, so a
#      localhost POSTGRES_DSN in your .env is correctly overridden here.

x-api-env: &api-env
  POSTGRES_DSN: postgresql+psycopg://evomind:${POSTGRES_PASSWORD:-evomind}@postgres:5432/evomind
  REDIS_URL: redis://redis:6379/0
  QDRANT_URL: http://qdrant:6333
  NEO4J_URI: bolt://neo4j:7687
  NEO4J_USER: neo4j
  NEO4J_PASSWORD: ${NEO4J_PASSWORD:-evomind123}
  PRIMARY_PROVIDER: ${PRIMARY_PROVIDER:-nvidia}
  EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER:-nvidia}
  NVIDIA_API_KEY: ${NVIDIA_API_KEY:-}
  NVIDIA_BASE_URL: ${NVIDIA_BASE_URL:-https://integrate.api.nvidia.com/v1}
  NVIDIA_MODEL: ${NVIDIA_MODEL:-deepseek-ai/deepseek-v4-pro}
  NVIDIA_EMBEDDING_MODEL: ${NVIDIA_EMBEDDING_MODEL:-nvidia/nv-embedqa-e5-v5}
  NVIDIA_THINKING: ${NVIDIA_THINKING:-true}
  NVIDIA_REASONING_EFFORT: ${NVIDIA_REASONING_EFFORT:-max}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}
  GEMINI_API_KEY: ${GEMINI_API_KEY:-}
  LLAMAPARSE_API_KEY: ${LLAMAPARSE_API_KEY:-}
  OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
  CONFIDENCE_THRESHOLD: ${CONFIDENCE_THRESHOLD:-0.55}
  # Security (off by default — see header)
  AUTH_ENABLED: ${AUTH_ENABLED:-false}
  API_KEYS: ${API_KEYS:-}
  CORS_ORIGINS: ${CORS_ORIGINS:-*}
  MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-50}
  RATE_LIMIT_ENABLED: ${RATE_LIMIT_ENABLED:-true}

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: evomind
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-evomind}
      POSTGRES_DB: evomind
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports: ["5432:5432"]
    mem_limit: 1g
    cpus: 1.0
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evomind"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports: ["6379:6379"]
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  qdrant:
    image: qdrant/qdrant:v1.12.0
    restart: unless-stopped
    ports: ["6333:6333", "6334:6334"]
    volumes:
      - qdrantdata:/qdrant/storage
    mem_limit: 1g
    cpus: 1.0
    # NOTE: the qdrant image ships no curl/wget, so there is no reliable
    # in-container HTTP healthcheck; the API depends on it with service_started.

  neo4j:
    image: neo4j:5.20
    restart: unless-stopped
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-evomind123}
      NEO4J_PLUGINS: '["apoc"]'
    ports: ["7474:7474", "7687:7687"]
    volumes:
      - neo4jdata:/data
    mem_limit: 1536m
    cpus: 1.0
    healthcheck:
      # cypher-shell ships inside the official image.
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p ${NEO4J_PASSWORD:-evomind123} 'RETURN 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s

  api:
    build:
      context: ./apps/api
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      <<: *api-env
    volumes:
      - ./data:/app/data
    ports: ["8000:8000"]
    mem_limit: 2g
    cpus: 2.0
    healthcheck:
      # The api image is Python-only (no curl/wget), so probe via urllib.
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/api/health', timeout=4).status==200 else 1)\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s
    depends_on:
      postgres: { condition: service_healthy }
      redis:    { condition: service_healthy }
      qdrant:   { condition: service_started }
      neo4j:    { condition: service_healthy }

  worker:
    build:
      context: ./apps/api
    restart: unless-stopped
    command: celery -A app.workers.celery_app.celery worker --loglevel=info --concurrency=2
    env_file:
      - path: .env
        required: false
    environment:
      <<: *api-env
    volumes:
      - ./data:/app/data
    mem_limit: 2g
    cpus: 2.0
    depends_on:
      api: { condition: service_healthy }

  beat:
    build:
      context: ./apps/api
    restart: unless-stopped
    command: celery -A app.workers.celery_app.celery beat --loglevel=info
    env_file:
      - path: .env
        required: false
    environment:
      <<: *api-env
    mem_limit: 512m
    cpus: 0.5
    depends_on:
      api: { condition: service_healthy }

  web:
    build:
      context: ./apps/web
      args:
        # server-side only: used by next.config.mjs rewrites() at build time to
        # proxy /api/* to the backend over the compose network. Must NOT be
        # NEXT_PUBLIC_*, or it leaks into the browser bundle where "api" can't
        # resolve. Leave NEXT_PUBLIC_API_URL unset so the client calls the
        # same-origin /api path instead.
        API_PROXY_TARGET: http://api:8000
    restart: unless-stopped
    ports: ["3000:3000"]
    mem_limit: 1g
    cpus: 1.0
    depends_on:
      api: { condition: service_healthy }

volumes:
  pgdata:
  qdrantdata:
  neo4jdata:
